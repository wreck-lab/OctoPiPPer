KLIPPER_VAR="This is a module variable"
[ -n "$KLIPPER_SRC_DIR" ] || KLIPPER_SRC_DIR=/home/pi/klipper
[ -n "$KLIPPER_PYTHON_DIR" ] || KLIPPER_PYTHON_DIR=/home/pi/klippy-env

[ -n "$KLIPPER_REPO_SHIP" ] || KLIPPER_REPO_SHIP=https://github.com/wreck-lab/klipper.git
[ -n "$KLIPPER_REPO_BRANCH" ] || KLIPPER_REPO_BRANCH=master
[ -n "$KLIPPER_REPO_COMMIT" ] || KLIPPER_REPO_COMMIT=v0.10.0

[ -n "$KLIPPER_PHAT_VERSION" ] || KLIPPER_PHAT_VERSION=v2
